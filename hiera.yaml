---
version: 5
hierarchy:
  - name: "FQDN"
    paths:
      - "nodes/%{trusted.certname}.yaml"
      - "nodes/%{facts.fqdn}.yaml"
  - name: "Role"
    path: "roles/%{facts.role}.yaml"
  - name: "Defaults"
    path: "common.yaml"
